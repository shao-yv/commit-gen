# 开发日志

## 2026-03-21（第1天）
### 今日完成
- 环境搭建：安装 Python 3.13、VS Code、Git。
- 注册硅基流动账号，获取 API Key。
- 编写 `test_api.py`，成功调用第一个 API 响应。
- 编写 `commit_gen.py` 核心功能：读取 Git diff → 调用 API → 输出提交信息。
- 完善错误处理：增加 API 重试机制（3次）、超时设置（120秒）。
- 将 API Key 移入 `.env` 文件，使用 `python-dotenv` 加载，并添加 `.gitignore` 避免泄露。
- 在本地初始化 Git 仓库，完成首次提交。

### 遇到的问题
- API 返回 401 认证错误：Key 未正确加载。
- 模型响应超时：GLM-5 速度慢，影响调试。
- 之前 API Key 硬编码在代码中，有泄露风险。

### 解决方案
- 使用 `python-dotenv` 从 `.env` 读取 Key，解决认证问题。

### 解决方案
- 创建 `.env` 文件存放 Key，代码中通过 `load_dotenv()` 读取，并更新 `.gitignore`。
- 增加 API 重试机制（3次）、超时设置（120秒）。

### 下一步计划
- 学习 Markdown，编写项目计划书和开发日志。
- 将代码推送到 GitHub 远程仓库。
- 进一步丰富命令行功能

## 2026-03-23（第3天）

### 今日完成
- 为 `commit_gen.py` 增加 API 调用失败时的降级方案：当所有重试失败后，返回通用提交信息（`chore: 更新代码（AI 服务暂时不可用，请手动补充说明）`）。
- 在 `main` 函数中添加对降级信息的友好提示。
- 测试命令行参数 `-p/--path` 和 `-s/--stage` 的功能，通过创建临时 Git 仓库验证。
- 编写并完善 `README.md` 项目说明文档，包含功能特性、安装使用、命令行参数、示例、技术栈等。
- 将本地代码推送到 GitHub 远程仓库 `commit-gen`，完成版本管理初始化。

### 遇到的问题
- 推送时因远程仓库地址拼写错误（`commit_gen` vs `commit-gen`）导致 `repository not found`，修正后成功。
- 图片文件较大导致推送中断，决定暂不推送图片，仅推送代码和文档。
- 编写 README 时部分 Markdown 格式不清晰，最终使用规范的三级标题和代码块解决。

### 解决方案
- 使用 `git remote set-url origin` 修正远程地址。
- 将图片文件从暂存区移除，后续单独处理。
- 参考标准 Markdown 规范重新组织 README 结构，确保层级清晰。

### 下一步计划
- 进一步丰富功能和细节

## 2026-03-24（第4天）

### 今日完成
- 与知南梳理了项目现状，明确当前代码量偏少、技术深度不足，需要从“能跑”向“有深度”深化。
- 确定了三条后续主线：
  1. 代码模块化重构，同时为克隆检测预留接口；
  2. 探索 RAG 技术，提升提交信息质量；
  3. 系统学习相关技术知识，从被动接受到主动理解。
- 反思了前期过度依赖 AI 生成代码的问题，决定从逐行理解现有 `commit_gen.py` 开始，夯实基础。
- 制定了近期学习计划：重点攻克 Python 基础、Git 原理、命令行参数、API 调用机制、模块化设计、RAG 入门概念。

### 遇到的问题
- 无

### 解决方案
- 无

### 下一步计划
- 今明两天：逐行阅读 `commit_gen.py`，用注释写下每段代码的作用，标记不懂的地方。
- 周五：开始模块化重构，按规划拆分代码。
- 同步阅读综述论文的评估协议部分，为后续实验设计做准备。

## 2026-03-27（第5天）

### 今日完成
- 确认项目模块化结构正常运行，为 RAG 集成做好准备。
- 安装 `datasets` 库，用于加载 CommitBench 数据集。
- 编写 `prepare_data.py` 脚本，实现从 Hugging Face 加载 CommitBench 训练集，筛选 Python 语言的 diff 和提交信息，保存为 `retrieval_data.json`。
- 理解数据筛选流程：通过 `load_dataset` 加载数据，遍历记录，按 `language` 字段过滤，提取 `diff` 和 `message`。

### 遇到的问题
- 网络不稳定，数据集下载未完成，计划明天继续。

### 解决方案
- 无（待明天网络好转后重新运行脚本，支持断点续传）。

### 下一步计划
- 明天继续下载数据，完成 `retrieval_data.json`。
- 安装 `chromadb`，构建向量检索库。
- 在 `preprocessor.py` 中实现检索逻辑，集成 RAG。

## 2026-03-28（第6天）

### 今日完成
- 全量构建 ChromaDB 向量检索库，共 309864 条 Python 提交记录（diff + message）。
- 解决了构建过程中的网络中断、SSL 证书验证、ChromaDB 默认模型下载等问题，实现断点续传。
- 在 `preprocessor.py` 中实现 `retrieve_examples` 函数，连接 ChromaDB 并自动检索与当前 diff 最相似的 3 条历史示例。
- 修改 `prompt_builder.py`，将检索到的示例融入提示词，实现 RAG 增强。
- 测试 RAG 效果，生成的提交信息质量显著提升（更规范、更具体、符合项目风格）。

### 遇到的问题
- 网络不稳定导致全量构建中断多次，但断点续传机制保证进度不丢失。
- 获取已有集合时未指定 `embedding_function`，导致 ChromaDB 下载默认 ONNX 模型（已修复）。
- 嵌入 API 偶尔 SSL 证书验证失败，添加 `verify=False` 临时解决。

### 解决方案
- 在 `build_retrieval_db.py` 中实现进度文件记录，每次插入后保存当前索引，重启后自动恢复。
- 获取集合时明确传入相同的嵌入函数实例，避免回退到默认模型。
- 在 `requests.post` 中加入 `verify=False` 并禁用警告。

### 下一步计划
- 进一步优化增强相关功能
- 学习更多 RAG 优化技术（如重排序、混合检索）。

## 2026-03-29（第7天）
### 今日完成
- 全量向量库构建完成（30 万条 Python 记录），实现断点续传。
- 集成 RAG 检索到 `preprocessor.py`，自动检索相似历史示例。
- 修改 `prompt_builder.py`，将检索到的示例融入提示词。
- 测试 RAG 效果，生成的提交信息质量显著提升。

### 遇到的问题
- 全量构建时网络中断，断点续传机制确保进度不丢失。
- ChromaDB 获取集合时未指定 embedding_function 导致下载默认模型，已修复。

### 下一步计划
- 进一步优化增强相关功能
- 学习更多 RAG 优化技术（如重排序、混合检索）。
- 优化代码结构为软著做准备